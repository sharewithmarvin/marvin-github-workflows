name: java-graalvm-pull-request

on:
  workflow_call:

jobs:
  TEST:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: "☕️ Set up JDK 11 for x64"
        uses: actions/setup-java@v3
        with:
          java-version: '11'
          distribution: 'adopt'
          architecture: x64
      - name: "📸 Build & ✅ Verify Unit Test" 
        run: mvn --batch-mode --update-snapshots package
